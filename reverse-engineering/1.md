## Challenge name: JVM Reverse Engineering


<img width="1127" height="191" alt="image" src="https://github.com/user-attachments/assets/4249685d-c287-49ff-8752-8dc88b25498d" />
<br>
<br>

```Room Link:``` [https://tryhackme.com/room/jvmreverseengineering](https://tryhackme.com/room/jvmreverseengineering)
<br>
<br>

---

### Task 1 : Introduction

**Read the things told there**

**Questions**

**2) Consider the following bytecode:**

**LDC 0**

**LDC 3**

**SWAP**

**POP**

**INEG**

**Which value is now at the top of the stack?**

```
LDC 0
Stack: [] → [0]
        └── loads 0 into the empty stack

LDC 3
Stack: [0] → [3, 0]
        └── loads 3 onto the stack

SWAP
Stack: [3, 0] → [0, 3]
        └── swaps the top 2 values

POP
Stack: [0, 3] → [3]
        └── removes the top value (0)

INEG
Stack: [3] → [-3]
        └── negates the integer at the top of the stack
```

> -3

**3) Which opcode is used to get the XOR of two longs? (answer in lowercase)**

> lxor

**4) What does the -v flag on javap stand for? (answer in lowercase)**

> verbose

---

### Task 2 : Simple Hello World

**Download the given task files to cont..**

**1) Find the name of the file that this class was compiled from (AKA Source File)**

```
skid@TheGreen:~/ctfs$ ls
Main_1584042869908.class
skid@TheGreen:~/ctfs$ javap Main_1584042869908.class
Compiled from "SecretSourceFile.java"
class Main {
  Main();
  public static void main(java.lang.String[]);
}
skid@TheGreen:~/ctfs$
```

> SecretSourceFile.java

**2) What is the super class of the Main class? (Using internal name format, i.e. /)**

```
skid@TheGreen:~/ctfs$ javap -c -v Main_1584042869908.class
Classfile /home/skid/ctfs/Main_1584042869908.class
  Last modified Aug 7, 2026; size 438 bytes
  SHA-256 checksum 0cbf8b75eb0083b8929cd20b5544791be35321ec707655fa036b149be55a49cd
  Compiled from "SecretSourceFile.java"
class Main
  minor version: 0
  major version: 52
  flags: (0x0020) ACC_SUPER
  this_class: #5                          // Main
  super_class: #6                         // java/lang/Object  <- this is the ans as format told / insted of java.lang.object
```

> java/lang/Object

**3) What is the value of the local variable in slot 1 when the method returns? (In decimal format)**

```
Look at public static void main(java.lang.String[]);
 Code:
      stack=2, locals=2, args_size=1
         0: iconst_0
         1: istore_1
         2: getstatic     #2                  // Field java/lang/System.out:Ljava/io/PrintStream;
         5: ldc           #3                  // String Hello World
         7: invokevirtual #4                  // Method java/io/PrintStream.println:(Ljava/lang/String;)V
        10: iinc          1, 2
        13: return

First, `iconst_0` pushes the value `0` onto the operand stack, and `istore_1` stores that value in local variable slot `1`.
At the end, `iinc 1, 2` increments the value in local variable slot `1` by `2`, so the value changes from `0` to `2`.
```

> 2

---

